# Local daily logs (personal, not versioned)
logs/*.md
!logs/.gitkeep

# Don't commit github config with field IDs
# (User should run setup on their machine)
# Uncomment if you want to keep config local:
# memory/github-config.md
